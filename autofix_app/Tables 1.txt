create table public.app_limits (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  role text not null,
  "limit" integer not null default 0,
  constraint app_limits_pkey primary key (id),
  constraint app_limits_role_key unique (role)
) TABLESPACE pg_default;


------------------------------------------------------------------------------------

create table public.app_settings (
  setting_key text not null,
  setting_value jsonb not null,
  label text null,
  description text null,
  category text null default 'General'::text,
  display_order integer null default 0,
  input_type text null default 'toggle'::text,
  input_config jsonb null,
  default_value jsonb null,
  created_at timestamp with time zone null default now(),
  updated_at timestamp with time zone null default now(),
  constraint app_settings_pkey primary key (setting_key)
) TABLESPACE pg_default;

create index IF not exists idx_app_settings_category on public.app_settings using btree (category) TABLESPACE pg_default;

create index IF not exists idx_app_settings_display_order on public.app_settings using btree (display_order) TABLESPACE pg_default;

create trigger app_settings_updated_at_trigger BEFORE
update on app_settings for EACH row
execute FUNCTION update_app_settings_updated_at ();

---------------------------------------------------------------------------------------------------------------------


create table public.app_settings_backup_20241028 (setting_key text null, setting_value jsonb null) TABLESPACE pg_default;

----------------------------------------------------------------------------------------------------------------------------------------------

create table public.bookings (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  customer_name text not null,
  customer_phone text not null,
  pickup_address text null,
  scheduled_time timestamp without time zone null,
  assigned_pudo_id integer null,
  status text not null default 'Pending'::text,
  created_by_telecaller_id integer not null,
  constraint bookings_pkey primary key (id),
  constraint bookings_assigned_pudo_id_fkey foreign KEY (assigned_pudo_id) references users (id),
  constraint bookings_created_by_telecaller_id_fkey foreign KEY (created_by_telecaller_id) references users (id)
) TABLESPACE pg_default;

-----------------------------------------------------------------------------------------------------------------------------------

create table public.materials (
  id bigserial not null,
  name character varying(255) not null,
  category character varying(100) null,
  unit character varying(50) null,
  description text null,
  is_active boolean null default true,
  created_at timestamp without time zone null default CURRENT_TIMESTAMP,
  updated_at timestamp without time zone null default CURRENT_TIMESTAMP,
  "Guid" text null,
  "MasterID" bigint null,
  constraint materials_pkey primary key (id),
  constraint materials_guid_masterid_unique unique ("Guid", "MasterID"),
  constraint materials_name_key unique (name)
) TABLESPACE pg_default;

create index IF not exists idx_materials_name on public.materials using btree (name) TABLESPACE pg_default;

create index IF not exists idx_materials_category on public.materials using btree (category) TABLESPACE pg_default;

create index IF not exists idx_materials_active on public.materials using btree (is_active) TABLESPACE pg_default;

------------------------------------------------------------------------------------------------------------------------------------
create table public.owner_master (
  id uuid not null default gen_random_uuid (),
  "Owner name" text not null,
  alias text null,
  alias1 text null,
  alias2 text null,
  "Address Line1" text null,
  "Address Line2" text null,
  "Address Line3" text null,
  state text null,
  country text null default 'India'::text,
  pincode character varying(10) null,
  "PhoneNumber" character varying(20) null,
  "MobileNumber" character varying(20) null,
  email text null,
  pannumber character varying(15) null,
  "GST Number" character varying(20) null,
  created_at timestamp with time zone null default now(),
  updated_at timestamp with time zone null default now(),
  "MasterID" bigint null,
  "Guid" text null,
  constraint owner_master_pkey primary key (id),
  constraint owner_master_guid_masterid_unique unique ("Guid", "MasterID")
) TABLESPACE pg_default;

---------------------------------------------------------------------------------------------------------------

create table public.reports (
  id serial not null,
  vehicle_id integer null,
  executive_id integer null,
  complaint text null,
  suggested text null,
  approved text null,
  status character varying(20) null,
  created_at timestamp without time zone null default now(),
  expected_delivery date null,
  marks jsonb null,
  odometer_reading integer null,
  "Owner name" character varying(100) null,
  client_phone character varying(20) null,
  approval_message_id text null,
  materials_required jsonb null,
  customer_feedback_text text null,
  customer_feedback_audio text null,
  gdrive_folder_url text null,
  service_feedback text null,
  inspector_id integer null,
  inspection_date timestamp without time zone null,
  inspection_remarks text null,
  photo_urls jsonb null,
  created_by_pudo_id integer null,
  booking_id bigint null,
  completed_at timestamp without time zone null,
  feedback_status text null,
  feedback_sent_at timestamp without time zone null,
  feedback_sent_by integer null,
  labour_cost numeric null default 0,
  constraint reports_pkey primary key (id),
  constraint reports_created_by_pudo_id_fkey foreign KEY (created_by_pudo_id) references users (id),
  constraint reports_executive_id_fkey foreign KEY (executive_id) references users (id) on delete set null,
  constraint reports_feedback_sent_by_fkey foreign KEY (feedback_sent_by) references users (id),
  constraint reports_inspector_id_fkey foreign KEY (inspector_id) references users (id) on delete set null,
  constraint reports_vehicle_id_fkey foreign KEY (vehicle_id) references vehicles (id),
  constraint reports_booking_id_fkey foreign KEY (booking_id) references bookings (id),
  constraint reports_status_check check (
    (
      (status)::text = any (
        array[
          'Not Started'::text,
          'Ongoing'::text,
          'Completed'::text,
          'Cancelled'::text,
          'Sent To Wash'::text,
          'Wash Completed'::text,
          'pending_inspection'::text,
          'inspection_approved'::text,
          'rejected'::text,
          'Delivered'::text,
          'Ready for Inspection'::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

create index IF not exists idx_reports_created_by_pudo on public.reports using btree (created_by_pudo_id) TABLESPACE pg_default;

create index IF not exists idx_reports_booking on public.reports using btree (booking_id) TABLESPACE pg_default;

create index IF not exists idx_reports_feedback_status on public.reports using btree (feedback_status) TABLESPACE pg_default;

create index IF not exists idx_reports_completed_at on public.reports using btree (completed_at) TABLESPACE pg_default;

------------------------------------------------------------------------------------------------------------------------------------

create table public.service_reminders (
  id bigint generated always as identity not null,
  report_id integer not null,
  vehicle_id integer not null,
  customer_name text not null,
  customer_phone text not null,
  vehicle_number text not null,
  vehicle_brand text not null,
  vehicle_model text not null,
  last_service_date timestamp with time zone not null,
  next_service_due_date date not null,
  follow_up_status text not null default 'pending'::text,
  notes text null,
  contacted_at timestamp with time zone null,
  created_at timestamp with time zone not null default now(),
  updated_at timestamp with time zone null default now(),
  constraint service_reminders_pkey primary key (id),
  constraint service_reminders_report_id_fkey foreign KEY (report_id) references reports (id) on delete CASCADE,
  constraint service_reminders_vehicle_id_fkey foreign KEY (vehicle_id) references vehicles (id) on delete CASCADE,
  constraint service_reminders_follow_up_status_check check (
    (
      follow_up_status = any (
        array[
          'pending'::text,
          'contacted'::text,
          'scheduled'::text,
          'no_response'::text,
          'completed'::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

create index IF not exists idx_service_reminders_next_service_due_date on public.service_reminders using btree (next_service_due_date) TABLESPACE pg_default;

create index IF not exists idx_service_reminders_follow_up_status on public.service_reminders using btree (follow_up_status) TABLESPACE pg_default;

create index IF not exists idx_service_reminders_customer_phone on public.service_reminders using btree (customer_phone) TABLESPACE pg_default;

create index IF not exists idx_service_reminders_vehicle_number on public.service_reminders using btree (vehicle_number) TABLESPACE pg_default;

create trigger trigger_update_service_reminders_updated_at BEFORE
update on service_reminders for EACH row
execute FUNCTION update_service_reminders_updated_at ();

----------------------------------------------------------------------------------------------------------------------------------------------------------


create table public.users (
  id serial not null,
  username character varying(50) not null,
  password character varying(100) not null,
  role character varying(20) not null,
  team text null,
  constraint users_pkey primary key (id),
  constraint users_username_key unique (username),
  constraint users_role_check check (
    (
      (role)::text = any (
        (
          array[
            'admin'::character varying,
            'executive'::character varying,
            'washer'::character varying,
            'inspector'::character varying,
            'tele_caller'::character varying,
            'pickup_dropoff'::character varying
          ]
        )::text[]
      )
    )
  )
) TABLESPACE pg_default;


------------------------------------------------------------------------------------------------------

create table public.vehicle_models (
  id serial not null,
  brand character varying(50) not null,
  "Model name" character varying(50) not null,
  "MasterID" bigint null,
  "Guid" text null,
  constraint vehicle_models_pkey primary key (id),
  constraint vehicle_models_brand_model_key unique (brand, "Model name"),
  constraint vehicle_models_guid_masterid_unique unique ("Guid", "MasterID")
) TABLESPACE pg_default;


-----------------------------------------------------------------------------------------------

create table public.vehicles (
  id serial not null,
  "Vehicle Number" character varying(15) not null,
  model_id integer null,
  fuel_type character varying(20) null,
  odometer integer not null,
  "Owner name" text null,
  "MobileNumber" character varying(20) null,
  vehicle_name character varying(100) null,
  "Color" character varying(50) null,
  "Engine Number" character varying(50) null,
  "Chasis Number" character varying(50) null,
  manufacturing_year integer null,
  "Model name" text null,
  owner_id uuid null,
  "MasterID" bigint null,
  "Guid" text null,
  constraint vehicles_pkey primary key (id),
  constraint vehicles_engine_no_key unique ("Engine Number"),
  constraint vehicles_chassis_no_key unique ("Chasis Number"),
  constraint vehicles_guid_masterid_unique unique ("Guid", "MasterID"),
  constraint vehicles_vehicle_no_key unique ("Vehicle Number"),
  constraint vehicles_model_id_fkey foreign KEY (model_id) references vehicle_models (id),
  constraint vehicles_fuel_type_check check (
    (
      (fuel_type)::text = any (
        array[
          ('Petrol'::character varying)::text,
          ('Diesel'::character varying)::text,
          ('CNG'::character varying)::text,
          ('Electric'::character varying)::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

create trigger trg_sync_vehicle_model_name BEFORE INSERT
or
update OF model_id on vehicles for EACH row
execute FUNCTION sync_vehicle_model_name ();

create trigger trg_sync_vehicle_owner_details BEFORE INSERT
or
update OF owner_id on vehicles for EACH row
execute FUNCTION sync_vehicle_owner_details ();